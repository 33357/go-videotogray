console.log("decodeGP")

function decodeGP(array,config){
    let grayArray=[]
    for (let i=0;i<config['outWidth']/(config['colorSize']+1)*config['outHeight']/(config['colorSize']+1);i++){
        
    }
}